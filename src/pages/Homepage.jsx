import React, { Fragment } from 'react';
import Banner from '../Components/Banner/Banner';

const Homepage = () => {
    return (
        <Fragment>
            <Banner/>
        </Fragment>
    );
};

export default Homepage;